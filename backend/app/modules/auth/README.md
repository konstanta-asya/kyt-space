# auth — Core Logic & Auth (Володимир)
Модель `users` (з роллю-enum на 6 ролей), реєстрація, логін,
хешування паролів (bcrypt/argon2), JWT, перевірка прав за роллю.

Очікувані файли: models.py, schemas.py, service.py, router.py
