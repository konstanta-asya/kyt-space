# ER-діаграма

> TODO: побудувати команді. Формат — Mermaid `erDiagram`, щоб діаграма
> версіонувалась у git разом із кодом.

Сутності-кандидати (з ТЗ): users, rooms, stationary_equipment,
portable_equipment, bookings, booking_equipment (junction),
blocked_slots (форс-мажори), admin_shifts.

```mermaid
erDiagram
    %% заповнити після рішень по моделі даних
```
